name: htmlproofer

on:
  push:
    branches:
      - htmlproofer
      - master

  workflow_dispatch:
    inputs:
      branchname:
        description: 'Run htmlproofer on branch'
        default: 'htmlproofer'
        required: true


jobs:
  htmlproofer1:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Checkout ${{ github.event.inputs.branchname }}
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.inputs.branchname }}
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '2.7'
      - name: Build jekyll site
        run: |
          bundle install
          bundle update github-pages
          bundle exec jekyll build

      - name: Run HTMLProofer action
        run: |
          bundle exec htmlproofer  --root_dir './_site' --extensions '.html' --ignore_empty_alt --only_4xx  --ignore_urls "https://fonts.gstatic.com,https://fonts.gstatic.com/,https://www.linkedin.com,https://github.com/jku-vds-lab/rubiks-cube-demonstrator,https://www.amd.com/en/support,https://support.amd.com/us/gpudownload/Pages/index.aspx,dx.doi.org/10.33313/380/218"

          #branding:
          #icon: "check"
          #color: "green"

